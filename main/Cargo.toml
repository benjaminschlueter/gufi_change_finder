[package]
name = "gufi_change_finder"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.40", features = ["derive"] }
users = "0.11.0"
whoami = "2.1.1"
scoutwrap = { path = "../scoutwrap" }
highest_change_tree = { path = "../highest_change_tree" }
